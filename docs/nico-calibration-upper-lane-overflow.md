# 上部レーン / 上固定コメントのはみ出し修正

目的:
- `ue` コメントや上部付近のコメントが、描画テクスチャのパディング込みでプレイヤー外へはみ出さないようにする。

現状:
- `drawComment` はキャッシュテクスチャを `comment.y - paddingY` に描く。
- そのため固定 `ue` コメントの本文Yが画面内でも、テクスチャ上端だけが負値になり得る。
- `resolveStaticCommentOffset` で `ue` の最小Yに静的コメントのテクスチャ上パディングを加味する修正を入れた。
- sm6240144 の 03:01.99 付近では、固定 `ue` の `drawImage.y` が負にならないことを確認済み。
- v3.1.6 で通常固定コメントの予約高さを `fontSize` ベースに戻し、03:02 付近の「ゆうて いみや おうきむ」同時刻 `ue` 6行が縦に潰れる問題を改善した。

次に見ること:
- 大きな `ue big mincho ca` が多数積まれる 05:30 以降のドラゴン系CAを実測traceと突き合わせる。
- 上端はみ出しだけでなく、同一vposの多段 `ue` コメントが正しい行間で積まれるかを見る。
- `shita` は現時点では目立つはみ出しがないため、先に `ue` を優先する。
- 通常 `naka` が `ue` 呪文の上を横切る見え方は、教師スクリーンショットと照合してニコニコ実プレイヤー側も同様か確認する。

完了条件:
- 固定 `ue` コメントの `drawImage.y` が原則0以上になる。
- ただしニコニコ実プレイヤー側が意図的に一部を画面外に出すケースがあれば、実測に合わせて例外を判断する。

